version: "3.7"

services:

  app_proxy:
    environment:
      APP_HOST: will-orca-calibration_web_1
      APP_PORT: 80

  web:
    image: ghcr.io/williamcarlos2/will-orca-calibration:latest
    restart: on-failure
    stop_grace_period: 30s
